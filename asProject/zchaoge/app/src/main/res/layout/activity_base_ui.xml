<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:fitsSystemWindows="true"
    android:orientation="vertical">

    <include layout="@layout/title_base" />

    <RelativeLayout
        android:id="@+id/ll_baseActivity_contentView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white"
        android:orientation="vertical">
    </RelativeLayout>

    <include
        android:id="@+id/ll_baseActivity_noDataView"
        layout="@layout/no_data_view"
        android:visibility="gone" />

    <include
        android:id="@+id/ll_baseActivity_netwrokErrorView"
        layout="@layout/network_error_view"
        android:visibility="gone" />
</LinearLayout>

