<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval"
    android:useLevel="false">
    <!-- 填充颜色 -->
    <solid android:color="#00ffffff"></solid>

    <!-- 线的宽度，颜色灰色 -->
    <stroke
        android:width="3dp"
        android:color="@color/appThemeColor"></stroke>

</shape>